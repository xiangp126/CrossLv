cd ~
ssh-keygen -t rsa

cd .ssh
touch authorized_keys
chmod 600 authorized_keys

-- Allow only key login | CentOS
sudo vim /etc/ssh/sshd_config
#search in vim for 'Password'

-- DEMON BEGIN
Password Authentiaction no
-- DEMON END

sudo /etc/init.d/sshd restart
and wait ...
